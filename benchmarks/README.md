# Benchmarks

This folder contains deterministic public verification runs.

Contents:
- runs/ → raw run ledgers + hashes
- results/ → summarized public verification JSON

Purpose:
Allow external users to confirm BRC determinism and chain integrity.
